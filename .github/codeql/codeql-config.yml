name: "Agrona CodeQL Scanning"

queries:
  - uses: security-and-quality

query-filters:
  - exclude:
      id: java/jdk-internal-api-access
  - exclude:
      id: java/missing-override-annotation
  - exclude:
      id: java/confusing-method-signature
